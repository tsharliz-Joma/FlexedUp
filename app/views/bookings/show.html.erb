<h1> Show Path: booking_path @ bookings#show </h1>

<div>
    Session Booked With:
    <div class="trainerBookingImg">
        <%= image_tag @booking.trainer.image %>
    </div>
        <span class="black">On:</span> <%= @booking.datetime.strftime("%B %e, %Y") %> 
        <span class="black">At:</span> <%= @booking.datetime.strftime("%H:%M:%S") %>
    <hr>
        <div class="gif-padding">
            <span class="black">"<%= @booking.trainer.name %> At the ready "</span>
        </div>
    </div>

    <div class="gif-padding">
        <button class="btn btn-outline-dark">
            <%= link_to "Confirm", root_path %>
        </button>
    </div>
<div>
    



